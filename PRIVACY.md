# 隐私说明

更新时间：2026-08-30

## 本扩展处理什么

AI 私密编辑在受支持网页的可识别发送动作发生前，以及纯文本进入已验证聊天编辑器前，读取当前文本并在浏览器本地运行确定性脱敏规则。用户明确启用本地神经模型时，草稿还会在扩展自己的 offscreen 文档中运行随包分发的 ONNX 模型。当前主机范围为豆包、DeepSeek、元宝、ChatGPT、Claude、Gemini、Kimi、通义千问和文心一言；页面结构不符合精确适配条件时扩展不接管。

- 高风险 API 密钥、访问令牌、私钥和连接字符串，以及密码、密钥、令牌、CVV 等明确字段标签后的值：先阻止当前发送动作并提示类别/数量；确认类命中默认先提供“匿名化发送”，用户也可取消、选择本会话允许，或明确选择仅本次按原文发送。全角字符、兼容连字符、零宽与双向格式控制字符、特殊空格会在本地位置映射后参与检测。
- 可替换结构化信息：普通替换模式会把手机号、邮箱、无标签但校验有效的中国居民身份证号、银行卡和统一社会信用代码等替换为令牌文本，再通过网站自身已验证的发送控件提交一次。
- 规则扩展：明确的生日/出生日期字段、标准 MAC 地址、对公/收款/结算/开户账户会在本地识别；已知网盘或协作域名的分享路径/口令链接，以及聊天中 62 开头的 16 位银联号和带身份证语境的格式号码会作为需确认候选。普通发布日期、公开首页、订单号、端口和版本号语境不会触发这些补充规则。
- 明确标签字段与高信号上下文：姓名、地址、微信/QQ 等账号、身份证和银行卡，以及社保卡、合同号、车辆信息等，以“字段标签 + `:`/`：`/`=`/`是`/`为`/`叫` + 值”出现时按本地规则替换。两种检测范围都会识别有限高信号中文上下文，例如“负责人叫…、…负责、寄送到…、微信 ID 是…”，且这类候选必须经发送前确认。
- 本地统计模型：用户在“平衡 + 本地统计模型”下明确启用时，扩展先用中文关系、动作、姓名边界和地址组件生成高信号候选，再用随代码发布的冻结合成训练语料字符 n-gram 打分作为附加信号；该打分只能提高候选评分，不参与是否上报的门控，候选范围完全由规则与词表决定。训练与评测实体值分离，用户草稿不会用于训练。闭合代码块会跳过；未闭合示例只保护开头两行，后续正文继续走确认检测。所有语义候选必须走确认路径；类别策略、同类规则和重叠候选都不能将其降级为静默发送。确认框只显示“本地语义候选”和类别数量，不显示候选原文。它不调用远程模型，不是神经网络或通用中文语义理解；长草稿按重叠窗口继续扫描，仍可能漏检或误报。
- 本地神经模型：用户在“平衡 + 本地神经模型”下明确启用时，内容脚本会在输入停顿后把不超过 5,000 字的当前草稿通过扩展内部消息交给本机 service worker 和扩展自己的 offscreen 文档；该文档使用随包分发的 ONNX Runtime WASM 与 INT8 模型生成姓名、地址和账号别名候选，再把仅含位置和类别的结果回传当前内容脚本的内存缓存。发送时会停止原始事件并对最终草稿执行一次精确复核；模型不可用、复核失败或草稿过长时，扩展不会把空缓存当成无敏感信息，而是要求用户取消或明确选择仅本次按原文发送。私密编辑器也通过同一扩展内部路径请求结果；草稿、模型结果和模型权重不上传、不写入 `chrome.storage`、IndexedDB 或审计。所有模型候选均要求确认。
- 自定义敏感词：普通替换模式会把用户主动保存的字面量词条替换为 `[[CUSTOM_001]]` 等令牌。
- 白名单：普通精确值可优先于内置规则和自定义敏感词。API 密钥、访问令牌、私钥、连接串和明确凭证字段不会被全局白名单静默放行；此类值仅可由当前页面会话允许，或由未到期的站点范围条目放行。
- 类别策略：用户可把每个敏感类别设为自动匿名化、发送前确认或始终阻止；始终阻止不会提供单次原文绕过。
- 严格模式：在已验证发送路径命中可替换结构化信息或自定义敏感词时，先阻止当前发送并保留原文；确认类命中默认先提供“匿名化发送”，取消不发送，原文只有在明确选择风险按钮后才仅本次重放一次。
- 纯文本粘贴：扩展仅在目标是已验证编辑器时拦截纯文本粘贴，并在内容进入网页前插入匿名化结果；富文本粘贴、图片和未验证编辑器保持原有网页路径。
- 私密编辑器：用户从工具栏弹窗主动打开侧栏后，草稿在侧栏内存中检查，并用当前本地自定义词、类别策略、检测范围、替换方式和可选本地统计语义审查生成出站文本。每个内容脚本文档在内存中生成随机绑定键；侧栏回传站点、标签页、绑定键和出站文本，worker 仅接受扩展侧栏页面的消息并固定转发到该标签页，内容脚本会再次校验站点与绑定键后才写入出站文本。绑定键不含原文、不写入 DOM、存储或网络。草稿不会先写入目标网页输入框，也不会自动发送。确认类命中会明确要求“确认匿名化填入”，始终阻止策略不会提供填入动作。为保持该路径不因放行配置退化，侧栏不使用全局或站点白名单；未命中规则时出站文本仍可能与草稿相同，界面会明确提示。
- 附件发送：扩展只在适配器声明附件能力且输入区已验证时判断是否存在附件元素，可按设置提示、要求发送前确认，或先阻止本次动作再提供一次性继续；不读取附件名称、路径、类型或附件内容。若用户选择“发送前确认”或“阻止”而当前站点未声明附件能力，扩展会暂停本次发送并明确提示“无法核验附件状态”，只有用户一次性确认后才复用网站原生发送路径。未验证附件节点不提供扫描保证。
- 本地文件扫描：用户在扩展“保护”视图主动打开独立扫描页并选择文件后，TXT/Office/PDF 文本层和 PNG/JPEG/WebP 可在本地处理。扫描页只显示类别和数量；文本/Office 只在发现敏感项时生成脱敏文本副本，图片只在识别到敏感词框时生成打码 PNG。PDF 即使文本层可完整提取也始终标为部分扫描，因为图片、扫描页、批注、表单和嵌入内容未覆盖。扫描器不继承网页发送用的白名单或类别策略，因此“需复核”只由本地语义候选触发。任何部分扫描结果都需要用户明确确认当前识别结果或扫描范围后才开放副本下载；图片的需复核结果与解码几何不可信的图片不生成打码副本。OCR 文本、文件字节和处理副本不写入扩展存储、审计或网络。
- 回复令牌查看：用户显式开启且使用令牌替换时，当前页面上出现的已发送令牌可由用户点按短暂查看当前页面内存中的原值。令牌位置不依赖站点私有结构——ChatGPT 使用已取证的助手回复标记，其余站点按本扩展自己生成的令牌字面量定位，且只处理经当前页面会话映射确认过的令牌，因此不会装饰页面上的任意文本。可编辑区域（输入框）内的令牌一律不处理，避免在用户草稿中插入元素；用户自己已发出的消息气泡内的令牌也可点按，其原值本就是用户输入的内容。原值不写入网页 DOM 文本、扩展存储或网络；浮层位于扩展拥有的页面级关闭 Shadow DOM，并会自动清除。关闭的 Shadow DOM 只降低普通 DOM 文本暴露，不构成对恶意扩展、浏览器漏洞或操作系统级记录器的隔离边界。超长凭证不会在映射中保留原值，无法通过此功能恢复。
- 未命中内容：不修改输入框，网站按原有发送路径处理。

普通替换模式会在当前页面文档的内容脚本内存中维护一份短暂映射，使同一规范化匹配值在连续消息中复用同一令牌。此映射可能包含手机号去除空格和连字符后的形式、邮箱小写和兼容连字符后的形式、身份证号大写后的形式、明确标签字段的原始值，或用户自定义词条的原字面量；它只用于生成令牌，不会写入网页 DOM、扩展存储、service worker 或网络请求。超长凭证只保留不可逆的短标记，不保存原始值。

严格模式不会扩大适配范围。未适配页面或适配健康检查失败（找不到输入框、输入区结构与预期不符）时，本扩展不接管页面交互、不读取该次发送的词库快照、不检测草稿、不改写、不阻止、不重放，也不显示“已保护”提示；受支持但未验证的页面会显示“敏感文本可能按原文发送”的本地提示。健康检查以**输入框**为准：发送按钮在输入框为空时被禁用或不渲染属于站点的空闲状态，不构成失效，此时词库快照仍会在识别到的发送手势上使用。已注册域名上的内容脚本可能预加载本地词库配置；这不读取网页草稿，也不使未验证发送路径受到保护。

`yuanbao.tencent.com` 是当前的 Beta 适配范围：公开未登录页面已观察到 Quill 编辑器和锚点型发送控件，该控件在这一状态下始终置灰。健康检查不把置灰当作失效（置灰是输入框为空时的空闲状态，所有已支持站点都如此），但置灰状态下发不出消息：本扩展在真实发送那一刻单独判断控件可用性，不可用时停在“请手动发送”，不会代为点击。认证后的真实发送链路尚未在本环境完成验证，不应被理解为已经完成发布级保护承诺。

`gemini.google.com` 只识别当前 Quill 编辑器与其同一 `text-input-field` 容器内的发送按钮，不依赖页面显示语言。Gemini 空输入时可能不渲染该发送按钮。健康检查不因此判定失效：发送控件缺席时本扩展仍会挂上粘贴拦截、输入时检查与 Enter/表单提交拦截，但**不会**把任何点击识别为发送手势（没有可信的点击目标），也不会代为点击。若输入框已有文本而发送按钮仍不可解析，工具栏会显示 `!`，表示点击这条发送路径未通过验证。

`www.kimi.com`、`www.qianwen.com`、`yiyan.baidu.com` 和 `wenxin.baidu.com` 已完成公开页面的编辑器/发送控件结构取证；Kimi、通义千问和文心一言的认证后真实发送链路尚未在本环境完成手工验收，页面结构不符时扩展一律不接管。

提示文案只包含类别和数量，不包含用户原文或完整令牌文本。

## 工具栏保护状态

在受支持网页中，内容脚本会在页面加载和影响已验证输入区的结构变化时执行适配健康检查，并发送固定站点标识与 `checking`、`protected`、`unverified` 之一给扩展自身的本地 service worker。用户启用回复令牌查看时，状态还可包含回复容器数量、已绑定令牌数量和固定诊断状态；不包含令牌文本或原文。该 worker 使用发送者的标签页 ID 更新工具栏徽标和悬停标题：`ON` 代表当前已通过健康检查，`!` 代表当前发送路径未验证，无徽标代表检查中或未支持页面。完整导航或同一文档内的 URL 变更会清除该标签页的旧状态；为避免完整导航前的延迟状态重新写入旧徽标，worker 仅在内存中暂存浏览器提供的文档 ID，并在标签页关闭时清除。

此状态通道不传递聊天草稿、命中类别、令牌文本、Cookie、URL、页面标题或聊天记录；文档 ID 是浏览器随消息提供的内部标识，不包含页面内容，也不会写入 `chrome.storage.local`、`chrome.storage.sync` 或任何远程服务。工具栏状态不是对所有敏感信息都能发现的保证，也不改变“用户直接输入的原文会存在于网页 DOM”的边界。

## 本扩展不做什么

- 不上传文本、检测结果或自定义敏感词。
- 不调用后端、分析服务、遥测、广告或远程规则服务。
- 不使用 `chrome.storage.sync`、IndexedDB、Cookie 或本地文件持久化聊天草稿、检测结果或会话历史。
- 不读取 Cookie、会话令牌、账号信息或网页私有 API。
- 不读取标签页 URL、标题或页面文本来生成工具栏保护状态，也不请求 `tabs` 权限。
- 不拦截 `fetch`、XHR、WebSocket、HTTPS、Service Worker 或其他网页网络请求。
- 不构造、复制或重放网站网络请求。
- 不读取目标聊天网页中的附件、媒体、截图内容，不扫描已有聊天记录，也不提供任意自由文本的完整语义识别。独立扫描页只处理用户主动选择的单个本地文件，不会拦截网页上传。

## 本地文件扫描边界

文件扫描页不嵌入任何目标聊天网站。用户选择的文件、提取文字和 OCR 文本只在该扩展页及其本地 worker 内存中用于当前一次扫描；扫描摘要不写入本地审计，文件名、MIME、大小、页码和命中文本不会进入 `chrome.storage.local`。

- 文本、DOCX 和 XLSX 可生成纯文本脱敏副本，不重写原 Office 文件。PDF 仅提取最多 20 页文本层并报告覆盖范围，始终属于“部分扫描”，不生成 PDF 副本。PNG、JPEG 和 WebP 在扩展内置 OCR worker 中识别并按词框输出临时打码 PNG。图片带非正立 EXIF 方向、识别结果被截断，或解码几何与文件头声明不一致时都不输出副本，以免给出未真正遮挡的图片。该方向检查覆盖 JPEG 的 EXIF 标记；PNG 的 eXIf 块与 WebP 的 EXIF 块理论上也能携带方向，浏览器处理不一致且实际极少出现，若它们造成宽高互换仍由几何校验拦截，但镜像类方向无法检出。
- 单文件上限 10 MiB，可提取文本上限 2 MiB，图片上限 12 MP 且单边不超过 16,384 px，PDF 最多 20 页。压缩 Office 文件会在解压前检查路径、加密标记、条目数、重名条目、压缩比、未压缩总量、XML 预算，以及单个待提取 XML 条目的压缩数据量（512 KiB）；解析失败、超限或不支持的格式不会生成副本。其中压缩比与未压缩总量依据压缩包自身声明的数值，声明可以造假；压缩数据量限制用于约束最坏解压体积，但不能完全排除恶意压缩包耗尽扫描页内存。该情形仅影响扫描页自身（需重新打开），不会导致数据外泄或写入存储。
- 扫描结果分为“未发现已识别敏感项”“已发现敏感项”“部分扫描”和“无法扫描”。部分扫描不表示文件安全；所有 PDF 文本层检查、低置信 OCR、无文本层 PDF 以及 Office 未提取的内嵌媒体或对象均可能漏检。
- 扫描器不能保证阻止网站已经发起的上传。应先在扫描页处理文件，再手动上传处理副本；在聊天网页中选择原文件后，网站可能已经收到它。

## 自定义敏感词的本地保存

用户在浏览器工具栏弹窗中添加的自定义敏感词、固定分组、白名单、严格模式、类别策略、检测范围、替换方式、回复令牌查看开关、附件策略和审计开关会保存到 `chrome.storage.local`。词库、白名单和发送策略仅保存在此浏览器本地，不进行 Chrome 同步，也不会上传到本扩展或目标网站之外的任何服务。

词库最多保存 100 个词条，单条最长 120 个字符。词条按插入顺序保存，可标记为个人、工作、项目或其他；匹配为区分大小写的字面量精确匹配，不支持正则表达式、通配符或语义识别。用户可删除单条词，或在确认后清空全部词条；卸载扩展会由浏览器移除该扩展存储。

白名单与自定义词库独立管理，最多保存 100 个精确值，单项最长 120 个字符。白名单按字面量精确匹配；手机号空格/连字符和邮箱大小写等现有候选规范化仍用于比较。全局白名单不放行 API 密钥、访问令牌、私钥、连接串和明确凭证字段。加入白名单意味着匹配值可能按原文提交给目标网站，用户可逐条删除或确认后清空。一次性确认发送不会自动加入白名单。

白名单可以是全局范围或单站点范围。单站点范围在添加前需要明确确认，默认有效期为 1 天；发送时再次检查有效期。高风险值的永久或过期站点条目不会放行。“本会话允许”不写入上述持久配置，只保存在当前页面文档内存中，刷新、关闭标签页或用户选择清除会话映射后即丢失。

## 本地审计和配置迁移

本地审计默认关闭。用户显式开启后，扩展会在 `chrome.storage.local` 最多保存 100 条摘要事件，每条仅包含固定站点标识、发送或粘贴、结果（已匿名化、已确认原文、已取消或已阻止）和敏感类别数量。审计事件不包含原文、令牌、字段标签、聊天标题或聊天记录；用户可随时在弹窗清空，关闭开关后不再新增。

配置导出默认仅包含自定义词、类别策略、保护设置和审计开关，不包含白名单、审计记录、会话令牌或网页数据。只有用户主动勾选并再次确认，导出才包含白名单。由于导出文本本身可能含用户定义的敏感业务字面量和放行值，用户不应将其分享给无关人员。配置导入会先严格验证 JSON 结构和版本；只有用户确认后才会原子替换当前配置，验证失败或取消不会写入任何设置。

## 网页 DOM 边界

用户直接输入网页聊天框的原文存在于目标页面 DOM。目标页面脚本、页面草稿能力或恶意网页代码可能读取这段文本；扩展无法把网页输入框变成与网站隔离的私密编辑器。

私密编辑器是可选的替代输入路径：草稿在侧栏内存中检查，未处理草稿不会先进入目标网页 DOM。检查通过后，侧栏只把生成的出站文本交给已验证的当前站点、标签页和页面实例输入框，用户仍需在网页手动发送。它不构成对规则漏检、用户直接在网页输入、网页附件上传、恶意扩展或操作系统级记录的隔离承诺；若未命中规则，出站文本可与原草稿相同。

扩展本身不上传、不调用后端、不拦截网页网络请求。聊天草稿与检测结果不会持久化；启用本地神经模型时，草稿仅在当前内容脚本、service worker 和 offscreen 文档的瞬时内存中传递。用户主动保存的词库、策略和保护设置会保存在本地扩展存储。它只在已适配的发送手势前本地替换或阻断，不能保证覆盖目标页面的所有数据路径，也不能保证发现所有敏感信息。元宝 Beta、Kimi、通义千问和文心一言的认证发送路径在完成手工验收前属于额外限制。

## 自动发送时会发生什么

用户点击已验证发送按钮、按已验证的正常 Enter，或触发表单提交时，内容脚本会在捕获阶段先检查当前页面的适配健康状态，再检查文本。

- 普通替换成功：原始发送事件被阻止，令牌文本写回网页输入框；控件可用时仅触发一次网站自身发送控件。
- 确认类命中：原文留在输入框，当前发送先被阻止并显示类别/数量；确认框默认提供“匿名化发送”，会写入令牌并仅触发一次已验证发送控件；用户也可取消、选择本会话允许，或明确选择“仅本次发送原文”。
- 控件在替换后的重放前不可用或页面变化：令牌文本留在输入框，显示“已替换敏感信息，请手动发送”。扩展不重试，也不调用网站网络接口。
- 高风险凭证：原文留在输入框，当前发送先被阻止并显示类别/数量；默认匿名化发送，原文必须通过单独的风险按钮明确选择，且只对本次重放。
- 确认弹窗打开期间重复点击和重复 Enter 会被拦截；按 Escape、关闭页面或确认回调异常均按取消处理。一次性原文确认不会自动加入白名单；“本会话允许”只把本次发现的精确值放入当前页面内存。页面或草稿在等待期间发生变化时不重放，原文保留。
- 词库正在加载或读取失败：已识别的非空发送动作被停止，原文留在输入框；当附件状态无法核验时，空文本发送也会停止，避免把可能的仅附件发送当作空草稿放行。
- 未命中：不显示扩展提示，也不改变网站的原发送行为。
- 未适配或适配健康检查失败：不读取该次发送的词库快照、不检测文本、不改变网页事件；受支持但未验证的页面会显示本地提示，说明敏感文本可能按原文发送；该页面不应被理解为受到本扩展保护。

程序化发送控件只用于复用用户已发起的一次站点发送意图，不代表扩展代理或转发网页请求。

## 保存期限

扩展不持久化草稿、完整检测结果或会话历史。私密编辑器草稿和用于当前预览的令牌映射仅存在于已打开侧栏页面内存，成功填入、清空草稿、修改草稿或关闭侧栏后会清除，不写入 `chrome.storage`、审计或网络。本地神经模型的精确草稿缓存仅在当前页面内容脚本内存中保留，刷新、关闭标签页、扩展重载或草稿变化后失效；offscreen 文档不保存请求历史。自定义敏感词、固定分组、白名单、严格模式、类别策略和审计开关保存在 `chrome.storage.local`，直到用户删除、确认清空或卸载扩展；页面刷新、关闭标签页或扩展重载不会删除它。若用户开启本地审计，还会保存上述最多 100 条不含原文的摘要事件。文件扫描不写入审计或扩展存储；清除、替换文件或关闭扫描页后，浏览器会释放该页的文件、OCR 和处理副本内存。会话清除功能还会写入一个随机、无原文的本地清除信号，以通知已打开的受支持页面各自清空内存映射。该配置和摘要均不进行 Chrome 同步，也不上传。

会话稳定令牌映射不属于上述持久配置：它仅在当前页面文档的内容脚本内存中存在，刷新、关闭标签页、重载扩展或用户请求清除会话映射后会丢弃；站内 SPA 切换通常仍在同一页面文档中，因此会继续复用。这一映射不写入 `chrome.storage.local`、`chrome.storage.sync`、IndexedDB 或远程服务；超长凭证的原值不会进入此映射。

## 安全限制

规则、校验算法、有限中文上下文和本地神经模型都可能漏检或误检；本地模型不是通用中文语义理解。没有高信号上下文且模型缓存尚未就绪的姓名、地址、业务语义、手写图片、扫描型 PDF、Office 未提取的内嵌媒体或对象、语音和规则外的密钥格式可能无法识别。当前神经模型只补充姓名和地址候选，不覆盖全部 PII 类型，且超过 5,000 字的草稿不进入该模型路径。ChatGPT、Claude、Gemini 或国内站点的页面更新也可能使精确 DOM 选择器失效；图标显示 `!` 时页面不受该扩展接管。Claude、Kimi、通义千问和文心一言的认证后真实发送路径尚未在本环境完成验证。用户仍应审阅将要发送给目标网站的内容，并根据所在组织的数据分类要求和目标服务条款决定是否发送。

## 公开发布要求

此文件是源代码随附的隐私说明，不是已完成应用商店主体认证的正式隐私政策。公开发布前，发行方必须托管此说明为稳定的公开 URL，并补充发行主体名称、适用地区、联系渠道和适用法律要求的权利请求方式。
