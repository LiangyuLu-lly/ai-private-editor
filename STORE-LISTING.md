# Chrome Web Store 上架材料

提交表单里逐项可直接粘贴的内容。数字全部来自实测，来源标在括号里；改动出货模型或默认值之后必须回来核对。

经营主体注册、隐私政策公网 URL、商店开发者账号不在本仓库完成，清单见 `COMMERCIAL.md`。

素材由 `node scripts/capture-store-assets.mjs` 生成到 `release/store-assets/`，截图是在真实 Chrome
里装载 `dist` 之后拍的，未经拼接或修饰。

---

## 基本信息

**名称**（≤45 字符）

```
AI 私密编辑
```

**简短说明**（≤132 字符，商店列表页显示）

```
发送给 AI 前在本机处理身份证、手机号、邮箱等；姓名仅辅助、可能漏检。不联网、不上传草稿。
```

**类别**：`隐私与安全`（Privacy & Security）

**语言**：简体中文

---

## 详细说明

```
发送给国内常用 AI 前，先在这台电脑上处理能校验的敏感格式。

在豆包、DeepSeek、腾讯元宝、ChatGPT、Claude、Gemini、Kimi、通义、文心的已验证聊天框里，点发送或
粘贴纯文本时，扩展会在本机检查草稿。手机号、邮箱、校验通过的身份证号等可替换为令牌（例如
[[PHONE_001]]）；密钥等高风险项会先确认。姓名、地址、账号别名由本地小模型辅助识别，会漏、也会
多问一次，不能当作已经挡住所有人名。

■ 全部在本机完成
规则在页面里跑，语义模型在扩展离屏页跑。扩展不申请网络权限，草稿不上传、不记原文。这不等于网页
自己看不到你正在输入的字。

■ 比较稳的
手机号、邮箱、校验位合法的身份证号、Luhn 通过的卡号、护照号、车牌、统一社会信用代码、IP、本机路径、
API 密钥与令牌、私钥、连接串，以及「标签 + 冒号」后的编号。普通替换模式下，这类命中可以静默改成令牌。

■ 只是辅助的
人名、地址、账号别名：本地模型给出候选后一律要你确认；没认出来则不会提示，原文会按网站路径发出。
请把真实姓名、客户名加进自定义敏感词（最多 100 条，只存在本机），不要假设模型能替你挡住。

■ 识别到了你可以选
确认框只列出类别和数量，不展示原文。可选匿名化发送、仅本次原文、本会话允许，或取消。模型项会标
「本地语义候选」。模型没就绪、复核失败或草稿超过约 5,000 字时，不会假装已经扫过：只能取消，或明确
选一次「仅本次按原文发送」。

■ 其它
纯文本粘贴同样检查；富文本、图片、附件内容不拦。工具栏 ON 表示当前编辑器已接管，! 表示本页未保护。
部分站点的登录态发送仍需你用合成文本自行验收，未验证的页面不要当成已保护。

■ 不宣称的事
不是等保/个保法认证，不是 DLP，不保证找出全部敏感信息，尤其不保证人名。请勿把它当作可以安全粘贴
任意内容的许可。完整边界见扩展内 PRIVACY.md。
```

---

## 单一用途说明

```
在受支持的 AI 聊天网页上，于发送前在本地识别并替换用户草稿中的敏感信息。
```

---

## 权限理由（逐条填写）

| 权限 | 填写内容 |
|---|---|
| `storage` | 保存用户自定义敏感词、白名单与检测档位。仅使用本地 `chrome.storage.local` 与 `chrome.storage.session`，不使用 `chrome.storage.sync`，不同步、不上传。 |
| `sidePanel` | 在侧边栏提供"私密编辑器"，让用户在发送前查看并逐项确认将要替换的内容。 |
| `offscreen` | 在离屏文档中运行本地语义模型（ONNX Runtime WASM）。必须是扩展页面才能使用 manifest 声明的 `wasm-unsafe-eval`；service worker 空闲时会被销毁，模型会反复重载；内容脚本受所在网页的 CSP 限制，无法可靠加载 WASM。草稿文本不离开浏览器。 |
| 站点访问（11 个具体域名） | 仅在受支持的 AI 聊天站点注入内容脚本以完成发送前检查。使用明确的域名列表，不使用 `<all_urls>` 或通配主机。 |

**未申请且刻意不申请**：`tabs`（打开首次运行页用 `chrome.tabs.create`，该方法不需要此权限）、
`activeTab`、`scripting`、`webRequest`、`cookies`、`history`、`downloads`。

---

## 数据处理声明

在"隐私实践"表单里，全部数据类型都选**不收集**，并填写：

```
本扩展不收集、不传输、不出售任何用户数据。

· 检测（正则、词表、语义模型）全部在用户设备上完成。
· 语义模型权重随扩展包分发（assets/model/），OCR 与 WASM 运行时同样随包分发（vendor/），
  运行时不下载任何代码或数据。
· 扩展未申请任何网络权限，代码中不存在向外部服务发起请求的路径。
· 自定义敏感词、白名单与设置存储在 chrome.storage.local；运行时状态存储在 chrome.storage.session
  （浏览器关闭即清除）。均不使用 chrome.storage.sync。
· 本地审计日志默认关闭，开启后也只写入本机存储，且不记录敏感值原文。

完整说明见扩展包内的 PRIVACY.md。
```

**远程代码**：选择"不使用远程代码"。ONNX Runtime 的 WASM 与胶水、tesseract 的 WASM 与语言数据、
pdf.js worker、语义模型权重全部随包分发，路径分别在 `vendor/onnxruntime/`、`vendor/tesseract/`、
`vendor/pdfjs/`、`assets/model/`、`assets/tessdata/`。

---

## 素材

`release/store-assets/`：

| 文件 | 用途 | 内容 |
|---|---|---|
| `01-welcome.png` | 截图 1280×800 | 首次安装说明页，讲明默认启用了哪一档与如何调低 |
| `02-send-confirmation.png` | 截图 1280×800 | 真实站点上的发送拦截确认框，含规则命中与模型命中（标注"本地语义候选"） |
| `03-settings.png` | 截图 1280×800 | 设置页：自定义敏感词、检测档位、语义审查与模型运行时状态 |
| `04-self-check.png` | 截图 1280×800 | 本地模型自检结果，含加载耗时与逐条跨度比对 |
| `promo-small-440x280.png` | 宣传小图 | |
| `promo-large-1400x560.png` | 宣传大图 | |

建议的截图说明文字（商店允许为每张截图配文）：

1. 装好就能用，默认设置和它的代价都写在第一屏。
2. 点发送之前先确认。模型认出的项会标出来，认错时一键放行。
3. 加上你自己的敏感词，只存在这台设备上。
4. 一键确认模型真的在你的浏览器里运行。

---

## 当前版本事实与可引用范围

| 项 | 当前值 / 边界 |
|---|---|
| 扩展版本 | `0.4.4`（`private-composer-r9-send-gate-2026-09-07.1`） |
| 出货模型 | `r9-electra-chat-augmented/checkpoint-3798`，阈值 `0.15` |
| 离线检查点 | `artifacts/r9-electra-chat-augmented/checkpoint-3798` |
| 浏览器耗时、内存、包体、跨度一致性 | 必须针对当前 R9 包运行 `npm run test:browser` 与扩展内自检后再记录；不可引用旧 R8 数字 |
| 检测效果 | 见 `ml/docs/r9-model-card.md`；该文档的限制与评测边界必须随同阅读 |

**不要在商店文案里引用排名、“最强/第一”、旧 R8 性能或未经当前构建复测的数字。**
仓库的 `ml/docs/benchmark-protocol.md` 不支持任何排名主张。

---

## 提交前自查

- [ ] `npm run package` 通过，用 `release/` 里的 zip 上传（不要上传 `dist/` 目录本身）
- [ ] 按 `RELEASE.md` 第一节在真实浏览器里跑一次自检，留存报告
- [ ] `src/manifest.json` 的 `version` 已递增
- [ ] `src/diagnostics.ts` 里记录的产物名与 `dist` 实际打包的一致（打包检查会拦，但先确认一遍更快）
- [ ] 四张截图与当前界面一致（改过 UI 就重新生成）
- [ ] 权限列表与本文件的理由表一致（打包检查会比对 `PERMISSION_JUSTIFICATIONS`）
- [ ] 开发者账号已用经营主体实名；商店「隐私权政策」填了可公网打开的 URL（见 `COMMERCIAL.md`，本仓库不能代注册或代托管）
- [ ] 简短说明与详细说明未写「零泄露 / 合规认证 / 全部人名」
