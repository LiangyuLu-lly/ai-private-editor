# 商业化落地（人做的事 vs 仓库里的事）

模型停在出货 R9，不换 ONNX。本文件只解决「怎么对外卖、怎么免责、怎么验收站点」。

仓库**不能**代你完成：工商/个体户注册、Chrome 开发者账号实名、把 `PRIVACY.md` 放到公网 HTTPS。这三项没有公网 URL，商店表单过不了。

---

## 1. 你必须自己办的

| 项 | 做什么 | 做成什么样才算完 |
|---|---|---|
| 经营主体 | 个体户或公司，与开发者账号实名一致 | 营业执照名称 = 商店开发者名称 |
| 隐私政策 URL | 把当前 `PRIVACY.md` 原文托管到你控制的 HTTPS（GitHub Pages、自有站均可） | 无登录可打开；内容与包内 `PRIVACY.md` 同版本 |
| 联系渠道 | 商店与隐私页同一邮箱或页面 | 能收起到用户删除/询问 |
| Chrome 开发者注册 | 一次性注册费；隐私实践全选不收集 | 与 `STORE-LISTING.md`「数据处理声明」一致 |
| 登录态验收 | 用**合成文本**在自有账号走发送，见第 4 节 | 豆包、DeepSeek 打勾前不要写「已保护登录态」 |

上架粘贴文案已改在 `STORE-LISTING.md`：姓名写成辅助、可能漏检。

---

## 2. 对外话术（销售 / 商店 / 合同共用）

**可以说**

- 发送前在本机处理；草稿不上传到本扩展。
- 身份证、手机、邮箱、卡号、密钥等有格式或校验的类型，命中后可匿名化或先确认。
- 模型不可用时不会假装扫过，只能取消或明确原文发送。
- 自定义词、白名单只存在这台浏览器。

**不可以说**

- 零泄露、防止个人信息发给大模型（作为保证）、个保法/等保/ISO 认证、DLP、找出全部人名。
- 支持 Edge（未单独验收）。
- 元宝/Claude/Kimi/通义/文心「登录态已发布级验证」（结构取证 ≠ 认证发送验收）。

演示只用：`请联系 13800138000`、合成身份证/邮箱。不要用真实客户名测给客户看。

---

## 3. 企业免责提纲（给律师改，不是已签署合同）

用途：试点合同或采购附件。把方括号换成主体名。

1. **产品性质**  
   [产品] 是浏览器本地辅助工具，在已适配聊天页的可识别发送/纯文本粘贴路径上处理草稿。不是 DLP、不是安全审计结论、不构成对《个人信息保护法》或行业合规的保证。

2. **有效范围**  
   仅当工具栏为 ON、编辑器健康检查通过时，该次发送/纯文本粘贴才进入本地检测。图标为 `!`、未适配页、富文本粘贴、附件/图片内容、网页已发起的上传，均不在保护范围内。

3. **检测局限**  
   结构化类型依赖格式与校验，仍可能漏检或误检。姓名、地址、账号别名依赖本地小模型，为辅助识别：未检出时无提示，原文按网站路径提交。用户直接键入网页输入框的文字会存在于目标页 DOM，页面脚本可能读取。

4. **用户选择**  
   「仅本次按原文发送/粘贴」将使该次内容按网站原路径提交。[甲方] 若不允许原文绕过，须在配置中关闭相应类别的原文按钮或改用始终阻止，并自行管理终端。

5. **数据**  
   扩展不向 [乙方] 上传草稿或检测原文。自定义词与可选审计摘要只存在用户浏览器本地。甲方不得要求乙方提供用户聊天原文。

6. **责任上限**  
   因漏检、误检、用户点选原文、站点改版导致适配失效，或目标网站已持有 DOM 中原文而产生的损失，[乙方] 不承担间接损失；直接损失以合同约定的年费为上限（律师填写）。

7. **禁止转售承诺**  
   甲方不得在对外宣传中把本产品描述为「已认证合规」或「可保证敏感信息不进入大模型」。

试点功能（比换模型更值得写进报价）：强制确认、关闭原文绕过、本地审计开/关、配置导出给 IT。这些不依赖 T1。

---

## 4. 豆包 / DeepSeek 登录态验收（合成文本）

在**你自己的账号**、新对话、图标已是 `ON` 后做。不要用真实个人信息。完整交互清单仍以 `TRY-IT.md` 第 6 组为准；下面是商业化最低集。

**环境**

- [ ] `npm run build` 后加载的是当前 `dist`
- [ ] 检测档位平衡 + 本地神经模型
- [ ] Chrome（Edge 单列，本清单不覆盖）

**豆包（www.doubao.com 或 doubao.com）**

- [ ] 输入无害句，图标 `ON`，点击发送：网页出现一条原文消息，无确认框
- [ ] `请联系 13800138000，邮箱 li@example.com。` 发出去的是令牌，不是号码/邮箱
- [ ] `对接人：测试甲` 或神经姓名候选：弹出确认，类别含姓名或标签字段；取消则不发新消息
- [ ] `sk-proj-abcdefghijklmnopqrstuvwxyz1234567890ABCDEFGHIJ`：确认框，默认匿名化
- [ ] 纯文本粘贴一段无规则命中的短句：神经未完成时不能先插入原文；超时或失败只能取消或「仅本次按原文粘贴」
- [ ] 图标在清空输入导致发送钮消失后的行为符合 README（空闲 ≠ 适配失败）

**DeepSeek（chat.deepseek.com，不要用 /sign_in 页）**

- [ ] 同上四条：无匹配放行、手机邮箱令牌、确认类、密钥确认
- [ ] 登录页不被写入、不拦截

**任一项失败**

- 不要在商店写该站「登录态已保护」
- 记录当时图标是 ON 还是 `!`；`!` 是适配问题，不是模型问题

元宝/Claude/Kimi/通义/文心：可做同样合成集，但未打勾前销售材料里写成「公开页结构已适配，登录态需验收」。

---

## 5. 建议报价切分（不是定价指令）

| 档 | 给什么 | 不给什么 |
|---|---|---|
| 个人免费 | 商店包、本机规则+R9、自定义词 | SLA、零漏姓名 |
| 团队年费 | 配置导出、建议关闭原文绕过的策略说明、合成验收陪跑一次 | 云端分析、按调用计费、把聊天原文交给你 |

先 3–5 个试点再谈模型升级。25MB 随包编码器是下一技术波次，不是开张前提。
