# 逐项体验清单（扩展 0.4.4 / R9）

按顺序走一遍，每项都写清了**怎么做、该看到什么、看不到说明什么**。带 ⚠️ 的是安全相关，看到不对请停下来。

需要联网的只有第 6 组（真实站点）。其余全部在本机完成。

---

## 0. 装上

```powershell
npm install
npm run build
```

1. 打开 `chrome://extensions/`，右上角打开**开发者模式**。
2. 点**加载已解压的扩展程序**，选仓库里的 `dist` 目录。
3. **新开一个窗口。** 新装的扩展不会出现在装载之前就存在的窗口里，这一步不能省。

**该看到**：工具栏出现扩展图标，并且**自动弹出「发送前会在本机检查」说明页**。

弹出来的那一页就是第 1 项要检查的东西。

> 如果加载时报 `Cannot load extension with file or directory name __xxx`：`dist` 里残留了评测脚本的
> 临时文件。跑 `npm run build` 重建即可（构建会先清空 `dist`）。

---

## 1. 首次运行说明页

**为什么要有它**：这一版默认**开启**本地语义模型（检测档位"平衡" + 语义审查"本地神经模型"）。
输入期间会预热本地模型；真正点击发送或纯文本粘贴时，扩展仍会复核最终草稿，而不是只相信之前的缓存结果。

**检查**：

- [ ] 装完自动打开，标题是"发送前会在本机检查"
- [ ] 写明姓名可能漏检、漏了无提示，且不要出现旧版 81.2% / 2.8% 数字
- [ ] 写明默认打开的是"平衡"档 + "本地神经模型"
- [ ] 说明本地处理与发送前确认的边界，不把旧版本的评测数字当成当前 R9 承诺
- [ ] 有两个按钮：**改成更少提示**、**保持推荐设置**
- [ ] 「更少提示」旁写明：关掉模型后，没有提示词的人名几乎不会再提示
- [ ] 点"改成更少提示"，出现绿色成功提示；再打开扩展设置 → **保护** 页，检测档位应变成"保守"、
      语义审查变成"关闭"
- [ ] 改回来：把检测档位设为"平衡"，语义审查设为"本地神经模型"（后面的项目都需要这套设置）

**关掉了想再看**：地址栏输入 `chrome-extension://<你的扩展ID>/welcome.html`。扩展 ID 在
`chrome://extensions/` 上能看到。

---

## 2. 本地模型自检（最值得先跑的一项）

**入口**：点扩展图标 → 底部**本地模型自检**。

**做**：点**开始自检**，等几秒。

**该看到**：

- [ ] 状态行显示 **"全部通过：6/6 条与离线评测一致"**（绿色）
- [ ] 环境表里 `offscreen 文档就绪` = **是**
- [ ] `模型产物` = `r9-electra-chat-augmented/checkpoint-3798`
- [ ] `offscreen 报告的错误` = **无**
- [ ] 记录本机显示的加载与请求耗时；不要拿旧 R8 的时间区间作为 R9 的通过条件
- [ ] 逐条比对 6 行全部"通过"，其中第一行 `周妍舒昨天说过这件事` 预期 `person_name 0-3`
- [ ] 点**复制报告**能拿到一段纯文本

⚠️ **如果 `offscreen 文档就绪` 是"否"或有错误**：模型没有在你的浏览器里跑起来。把报告贴出来。
在 `local_neural` 模式下，发送和纯文本粘贴都不会把这件事静默降级为原文放行：最终草稿复核失败或超过
15 秒仍无结果时，只能取消，或由你明确选择一次“仅本次按原文”。

⚠️ **如果 6 条里有单窗口的短句不一致**：说明浏览器与离线评测行为不同，属于缺陷，请贴报告。

旧 R8 曾记录过一组 Chrome 152 性能数字，但尚未作为 R9 的实测结果复核；当前请以本机自检报告留档。

---

## 3. 私密编辑器（不需要打开任何网站）

这是最快感受到检测效果的地方，因为它不依赖网页适配。

**入口**：点扩展图标 → 右上角**私密编辑器**（会打开侧边栏）。

**做**：把下面这段粘进"私密草稿"，点**检查并预览**。

```
客户交接：对接人周妍舒，手机 13843029571，寄样地址武汉市洪山区霁虹街95号，发票邮箱 zhou@example.test，
身份证 110105199003074210，工资卡 6222021234567890128，车牌 京A12345，护照 E12345678。
```

**该看到**（实测确认，8 项全中）：

- [ ] 八项全部变成占位符：`[[PERSON_001]]`、`[[CN_PHONE_001]]`、`[[ADDRESS_001]]`、`[[EMAIL_001]]`、
      `[[CN_ID_001]]`、`[[BANK_CARD_001]]`、`[[LICENSE_PLATE_001]]`、`[[PASSPORT_001]]`
- [ ] 同一个值出现两次时，两次的占位符编号相同

> **这段文本不能用来判断模型有没有起作用。** 里面写的是 `对接人周妍舒` 和 `寄样地址武汉市…`，
> 而 `对接人`、`寄样地址` 本身就是锚词，规则档就能接住；无锚词地址也是规则的能力（更早一轮加的
> 结构化识别）。想看模型的作用，用下一小节。

再试这几条，看**不该打码的东西没被打码**：

```
药明康德的报告已经发布。
李白将在公开课中被讨论。
公告提到黄鹤楼，不是个人联系信息。
示例中写的是张三，不是真人。
今天天气很好，适合出去走走。
```

- [ ] 这五条**都不应该有任何检测结果**

⚠️ 如果这五条里有被打码的，那是误报。上一版模型这五条里有四条会误报，这一版把负例词池从 14 个扩到
314 个之后应该都干净了。看到误报请告诉我是哪一条。

### 3b. 模型到底多出了什么（对照实验）

模型在产品里**唯一可见的独有贡献是裸人名**：常见姓 + 名，前面没有提示词，后面没有职务词。
其余类型规则都能接住。这一小节把它单独隔出来。

逐条输入，分别在**语义审查 = 本地神经模型**和**语义审查 = 关闭**下各检查一次：

| 输入 | 开模型 | 关模型 |
|---|---|---|
| `周妍舒昨天说过这件事。` | ✅ 认出 `周妍舒` | ❌ 什么都没有 |
| `王川梓昨天提交了复机申请。` | ✅ 认出 `王川梓` | ❌ 什么都没有 |
| `储翊安明天不在工位。` | ✅ 认出 `储翊安` | ❌ 什么都没有 |
| `武汉市洪山区霁虹街95号，明天上门。` | ✅ 认出地址 | ✅ **也认出**（这是规则的能力，不是模型） |

- [ ] 前三行的表现与上表一致——这就是模型的价值，也是"关掉它会漏掉什么"
- [ ] 第四行开关模型都认得出，说明地址不归模型管

以上四行我都在出货产物上实测过。前三行关掉模型时的检测结果是**完全空**，不是"少一项"。

---

## 4. 校验位是真的在校验

这一组用来确认"随手编的号码不会被打码"，避免把订单号当身份证。

在私密编辑器里逐条试：

| 输入 | 该不该打码 |
|---|---|
| `身份证 110105199003074210` | ✅ 应该（校验位合法） |
| `身份证 110105199003074211` | ❌ 不该（校验位错） |
| `工资卡 6222021234567890128` | ✅ 应该（过 Luhn） |
| `工资卡 6222021234567890127` | ❌ 不该（不过 Luhn） |
| `护照 E12345678` | ✅ 应该 |
| `订单 A12345678` | ❌ 不该（不是护照前缀） |

- [ ] 六条的表现与上表一致

---

## 5. 自定义敏感词、白名单、类别策略

**自定义敏感词**（扩展图标 → 第一个标签页）

- [ ] 加一个只有你知道的词，比如 `砚溪代号`，分组选"项目"
- [ ] 回到私密编辑器，输入 `内部代号是砚溪代号，别外传。`，检查 → 应该变成 `[[CUSTOM_001]]`
- [ ] 在词条列表里能搜到、能按分组筛选、能单条删除
- [ ] **严格模式**开关打开后，检测到敏感信息时的动作从"替换"变成"阻止"

**白名单**（第二个标签页）

- [ ] 把 `周妍舒` 加进白名单，作用范围选"所有支持网站"
- [ ] 回到私密编辑器检查第 3 项那段长文本 → `周妍舒` 这次不再被打码，其余照旧
- [ ] 试一次"指定网站" + 有效期"1 小时"，确认列表里显示了范围和到期
- [ ] 测完把它删掉，免得影响后面的项目

⚠️ 白名单里的高风险类别（密钥、令牌、私钥、连接串、密码）**故意不允许全局永久放行**。
可以试着把一个 `sk-` 开头的假密钥加进全局白名单，然后确认它**仍然会被打码**——这是有意的。

**类别策略**（第三个标签页）

- [ ] 把"姓名"的动作改成"发送前确认"，把"手机号"改成"阻止"
- [ ] 列表里能看到每一类当前的动作

---

## 6. 在真实站点上用（需要联网）

支持的站点：豆包、DeepSeek、腾讯元宝、ChatGPT、Claude、Gemini、Kimi、通义千问、文心一言。

**先看状态**：打开任意一个支持的站点。

- [ ] 工具栏图标上出现绿色 **ON** 徽标，悬停提示"当前页面已保护"

⚠️ 如果出现橙色 **!** 徽标并弹出"当前页面未验证（……）"，说明该站点的页面结构变了、扩展没认出输入框
或发送按钮。**此时扩展不能承诺接管发送；不要在该页面粘贴真实敏感信息。** 提示里括号内是具体原因
（找不到输入框 / 找不到发送按钮 / 输入区结构与预期不符），把它告诉我就能定位要改哪个选择器。

九个站点在**空输入框**上就应该直接是 `ON`，不需要先打字。站点在空闲时要么把发送按钮置灰（豆包、
DeepSeek、元宝、Kimi、通义千问、文心一言），要么干脆不渲染它（ChatGPT 换成语音按钮，Claude、Gemini
直接不插入）——两种都是空闲状态，不再被当成适配失效。九个站点的空输入框状态都在真实 Chrome 里逐个验证
过（`ml/reports/generated/r8/repro-empty-composer-all-sites.mjs`）。

如果空输入框就显示 `!`，那是真的结构变了，请把括号里的原因告诉我。另外，**输入框里已经有字**却显示
`!（找不到发送按钮）` 也是真问题：说明发送按钮的选择器失效了，此时点发送不会被接管。

**发送拦截与最终草稿复核**：在输入框里打字（可用第 3b 项的 `周妍舒昨天说过这件事。`），**不必等待**，
立即点发送。

> 想在这里也看到"（本地语义候选）"标记，草稿里要有**无锚词的裸人名**。第 3 项那段长文本里的
> `对接人周妍舒` 是靠锚词命中的，标记不会出现在它上面。可以把 `周妍舒昨天说过这件事。` 单独发一次。

- [ ] 原站点不会先收到原文；扩展先完成最终草稿的本地语义复核
- [ ] 复核成功且命中敏感信息后，弹出"检测到敏感信息"对话框
- [ ] 列出识别到的类别与项数，模型认出的项后面标着**（本地语义候选）**
- [ ] 有一行说明："带'本地语义候选'的项是模型的猜测，可能认错。如果它认错了，选'本会话允许'即可"
- [ ] 四个按钮都试一遍：
  - **匿名化发送** → 实际发出去的是占位符版本
  - **仅本次发送原文** → 原文发出，但白名单不变，下次同样会问
  - **本会话允许并发送** → 原文发出，且本次会话内这个值不再问（**误报时该点这个**）
  - **取消发送** → 什么都不发，草稿还在

> 输入时的防抖推理只用于预热。点击发送时，扩展会拦下本次提交并对**最终草稿**复核；不会因为缓存尚未写入
> 而直接按旧的“只走规则”路径发送。

**粘贴保护**：复制一段含手机号的文本，粘贴进输入框。

- [ ] 同样弹出确认对话框，按钮文案是"粘贴"而不是"发送"

本地神经模式下，未复核的剪贴板文本不得静默插入。规则不会命中的短句（例如 `今天只讨论接口设计，先整理需求，再安排测试。`）请放到第 11 组、模型不可用时验收：只能**取消粘贴**或**仅本次按原文粘贴**。

**附件提醒**：设置 → 保护 → 附件动作改成"发送前确认"，然后在站点上带附件发送。

- [ ] 未声明附件能力的站点会暂停发送并要求明确放行

**回复令牌还原**：需要 替换风格 = "令牌" 且 回复令牌查看 = 开。

- [ ] 用匿名化发送把带占位符的消息发出去，等 AI 回复里也出现 `[[PERSON_001]]` 这样的占位符时，
      占位符会变成可点的元素，点一下在页面内显示原值（只在内存里，不写盘，刷新即失效）

---

## 7. 替换风格

设置 → 保护 → **替换风格**，三种各试一次（在私密编辑器里最容易看）。

- [ ] **令牌**：`[[PERSON_001]]`，同值同编号，可配合回复还原
- [ ] **部分掩码**：保留少量字符，例如手机号变成 `138****9571` 形式
- [ ] **类型占位**：只说类型，不保留原值信息

---

## 8. 本地文件扫描

**入口**：扩展图标 → **本地文件扫描**。

- [ ] 选一个 `.txt`，里面写几行含手机号/邮箱/身份证的文本 → 列出命中项
- [ ] 选一个 `.docx` 或 `.xlsx` → 同样能扫出来
- [ ] 选一个文本层完整的 PDF → 能扫出来，且会说明"只覆盖文本层"
- [ ] 选一张含文字的 PNG/JPEG → 走本地 OCR，慢一些（首次要加载 OCR 引擎），能扫出图里的文字

⚠️ 文件不出设备。可以打开浏览器开发者工具的网络面板确认扫描期间没有任何外发请求。

---

## 9. 本地审计与配置搬迁

**审计**（扩展图标 → 第四个标签页）

- [ ] 打开开关，去做一次发送拦截，回来能看到记录
- [ ] 记录里**不含敏感值原文**（只有类别、时间、动作）
- [ ] "清空审计记录"可用

**配置搬迁**（第五个标签页）

- [ ] 点"生成配置"，得到一段文本
- [ ] 导入方式默认是"替换现有配置"；"清空全部"自定义敏感词后粘回文本点"导入"，确认后词条回来了
- [ ] 勾选"包含白名单"再导出一次，对比两次内容的差别
- [ ] 先在白名单里留一个值，导入方式改成"合并白名单"后再导入另一份含不同白名单值的配置：两个值都应还在，且不应弹出替换确认框

---

## 10. 隐私声明的可验证部分

这些是可以自己动手核实的，不用信我：

- [ ] `chrome://extensions/` → 本扩展 → **详情**，权限只有：存储、侧边栏、离屏文档，
      以及 9 个具体站点的访问权。**没有** "读取所有网站数据"
- [ ] 打开开发者工具网络面板，做一次完整的检测 + 发送拦截，确认**没有任何外发请求**
- [ ] 扩展目录里搜不到远程加载：模型在 `dist/assets/model/`，WASM 与 OCR 在 `dist/vendor/`，
      都随包分发
- [ ] `chrome://extensions/` 里禁用扩展后，站点行为完全恢复原生

---

## 11. 降级行为（可选，但值得看一次）

**模型不可用时的发送边界**：

```powershell
Rename-Item src\assets\model\model.int8.onnx model.int8.onnx.bak
npm run build      # 会打印一条 warning，说明模型未打包
```

重新加载扩展后（记得新开窗口）：

- [ ] 自检页显示模型未就绪并给出原因，而不是白屏或报错
- [ ] 在支持站点输入 `周妍舒昨天说过这件事。` 并点击发送，看到“本地语义复核未完成”
- [ ] 对话框只有**取消发送**与**仅本次按原文发送**；取消后草稿保留，明确选择后才允许原站点收到原文
- [ ] 不会出现模型未就绪却无提示、直接按纯规则放行原文的情况
- [ ] 同一设置下粘贴一段规则不会命中的短句，同样只能取消粘贴或“仅本次按原文粘贴”，原文不会先插入

复核时限无法在正常装载下人为触发：内容脚本与后台各 15 秒，超时走与上面相同的对话框，而不是发送或粘贴一直无提示。模型加载失败会有限次重试，不会因为一次瞬时失败就在该页面文档内永久关掉模型。

恢复：

```powershell
Rename-Item src\assets\model\model.int8.onnx.bak model.int8.onnx
npm run build
```

---

## 已知的不足（不用报，这些我知道）

- **R9 的泛化边界仍需按当前评测复核。** 旧 R8 对生僻姓与公众人物的观察保留在历史模型卡，不能作为 R9
  的量化结论。
- **超过 5,000 字的草稿不运行本地语义模型**。点击发送或纯文本粘贴后会显示“本地语义复核未完成”，要求你明确选择
  一次原文或取消；不会静默仅走规则后放行。
- **打完字立刻发送或立刻粘贴也不会跳过语义复核。** 输入期缓存只是预热，最终草稿会在发送或粘贴事件中重新复核。
- **复核有 15 秒失败关闭时限。** 内容脚本与 service worker 两侧都计时；超时与模型不可用走同一套取消 / 仅本次原文选择，不会让动作一直停住且无反馈。
- **"保守"档下模型不提供额外的语义候选**，语义审查开关也会被禁用。这是档位的定义，不是 bug。注意保守档
  **并非完全不认人名**——有锚词的（`对接人周妍舒`、`收件人：沈砚`）照样认得，只是裸人名认不出。
- **R9 模型的产品语义范围是 `PERSON`、`ADDRESS`、`ACCOUNT_ALIAS`。** 地址、账号以及所有有固定格式的类型
  仍由规则/校验器共同处理；当前 R9 的逐类效果必须以 R9 评测快照为准。
- **对话框不显示被标中的原文**，只说类别和数量。因为承载检测结果的数据结构刻意不携带原值——
  它会跨上下文传到侧边栏与后台，带上原值等于把敏感文本扩散到更多地方。
- **模型不可用、缓存未命中或复核超时都不会静默放行。** `local_neural` 模式对发送和纯文本粘贴都会要求你取消或明确选择一次原文；
  这不等于扩展能识别所有敏感信息，仍应在发送前检查草稿。
- **运行时清单缺字段会中止加载。** `threshold`、`max_length`、`stride`、`labels` 缺一不可，不会静默套用默认值。
- **模型加载失败会有限次重试**，不会因为一次瞬时失败就在该页面文档生命周期内永久禁用；用尽次数后仍走复核未完成，不会静默只走规则。

## 这份清单是怎么来的

R8 曾用 `ml/reports/generated/r8/verify-tryit-claims.mjs` 覆盖过部分旧行为；它不能证明当前 R9 的
模型、阈值或发送门控。第 2、6、11 组应以当前扩展包的自检报告、真实浏览器拦截结果和自动化回归共同验收。

写第一版时我有两处写错，都是脚本抓出来的，值得记下来免得再犯：

1. 长示例文本里写了 `对接人周妍舒`、`寄样地址武汉市…`，我拿它当"模型的功劳"——**错**，
   `对接人`/`寄样地址` 是锚词，规则档就能接住。
2. 我把无锚词地址也算成模型的贡献——**错**，那是更早一轮给规则加的结构化识别。

所以现在 3b 是唯一用来判断模型作用的一节，而且只用裸人名。

## 报问题时带上什么

1. 自检页的**复制报告**内容（第 2 项）。
2. 出问题的**具体输入文本**，以及你期望的结果。
3. 当前的检测档位与语义审查设置。
4. 如果是站点相关：站点名 + 工具栏徽标状态 + 提示里括号中的原因。

## 想看数字而不是体验

| 想知道 | 看哪里 |
|---|---|
| 当前 R9 模型范围与已知限制 | `ml/docs/r9-model-card.md` |
| 到底认得哪些类型，哪些还没做 | `ml/docs/type-coverage.md` |
| 评测时被丢掉的未映射检测种类 | 评测输出目录里的 `dropped-kinds.json`（不再静默丢弃） |
| 真实浏览器里的实测性能 | `RELEASE.md` 顶部，或自己跑 `npm run test:browser` |
| 隐私实现细节 | `PRIVACY.md` |
| 发布流程与打包 | `RELEASE.md` |
| 上架商店要填什么 | `STORE-LISTING.md` |
